{
  "name": "real-time-collab-editor",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run"
  },
  "workspaces": [
    "packages/shared",
    "packages/server",
    "packages/client"
  ],
  "devDependencies": {
    "tsx": "^4.23.0",
    "vitest": "^4.1.10"
  }
}
